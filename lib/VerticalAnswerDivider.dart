import 'package:flutter/material.dart';

class VerticalAnswerDivider extends StatelessWidget {
  const VerticalAnswerDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
      height: 1,
    );
  }
}
