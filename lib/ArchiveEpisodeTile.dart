import 'package:flutter/material.dart';

class ArchiveEpisodeTile extends StatelessWidget {
  const ArchiveEpisodeTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(

    );
  }
}
